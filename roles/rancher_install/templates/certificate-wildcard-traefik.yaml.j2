apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-{{ traefik_domain }}-traefik
  namespace: traefik
spec:
  secretName: wildcard-{{ traefik_domain }}-traefik-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "{{ traefik_domain }}.com"
  dnsNames:
  - '*.{{ traefik_domain }}.com'
  - "{{ traefik_domain }}.com"