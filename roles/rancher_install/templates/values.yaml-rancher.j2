hostname: "rancher.{{ traefik_domain }}.com"
ingress:
  enabled: true
  tls:
    source: secret

tls: "ingress"

replicas: 3

secrets:
  tlsSecret:
    name: wildcard-{{ traefik_domain }}-tls
    namespace: cattle-system